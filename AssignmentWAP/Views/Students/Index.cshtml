@model IEnumerable<AssignmentWAP.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaSinhVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HinhThucNopPhat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoTienNopPhat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayNopPhat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.MaSinhVien)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.HinhThucNopPhat)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SoTienNopPhat)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NgayNopPhat)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>
</tr>
}

</table>
